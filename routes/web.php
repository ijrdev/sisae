<?php

/* DB.
 * $query = DB::table('users')->get();
 * dd($query);
 */

/* CACHE.
 * Cache::store('redis')->put('linguagem', 'PHP');
 * dd(Cache::store('redis')->get('Linguagem'));
 */

/* SESSION.
 * session()->put('abc', '123');
 * session()->put('jogo', 'GTA V');
 * session()->put('idade', '23');
 * session()->save();
 * dd(session()->all());
 */
